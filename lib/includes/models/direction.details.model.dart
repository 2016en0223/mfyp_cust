class DirectionDetails {
  String? distanceText, durationText, polylinePoints;
  int? distanceValue, durationValue;

  DirectionDetails(
      {this.distanceText,
      this.distanceValue,
      this.durationText,
      this.durationValue,
      this.polylinePoints});
}
